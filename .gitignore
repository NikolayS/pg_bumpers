# Rust
/target/
**/target/
*.rs.bk

# Node (only used by deploy/up.sh for throwaway Ed25519 keypair gen; the MCP
# server is pure Rust — the TS mcp/server was removed in EPIC #83).
node_modules/

# Environment / secrets
.env
.env.*
!.env.example

# Clone / rehearsal artifacts (clone-orchestrator dry-run scratch, restore points, dumps)
/clones/
*.clone
*.restore-point
clone-artifacts/

# Local dev/test substrate — throwaway Postgres 18 clusters created by
# deploy/local-stack.sh (initdb/pg_basebackup data dirs + logs). Never commit.
/.localstack/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Generated GitHub Pages site (assembled by scripts/build-site.sh)
/site/
