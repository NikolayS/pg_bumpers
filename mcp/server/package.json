{
  "name": "@pg-bumpers/mcp-server",
  "version": "0.0.0",
  "private": true,
  "description": "pg_bumpers MCP server — the §4/§11 toolset, executing through the proxy (cooperative, NOT a security boundary; SPEC.md §3).",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "pgb-mcp": "dist/bin/mcpStdio.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "license-check": "node scripts/license-check.mjs"
  },
  "dependencies": {
    "pg": "8.22.0"
  },
  "devDependencies": {
    "@types/node": "22.18.0",
    "typescript": "5.6.3",
    "vitest": "2.1.9"
  }
}
